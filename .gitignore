*.pyc
*.pyo
.DS_Store
.env
.pytest_cache/
.venv/
__pycache__/
dashboard/index.html
node_modules/
telemetry/events.index
telemetry/events.jsonl
evals/datasets/labeled_captured.jsonl
telemetry/flywheel_pairs.jsonl
telemetry/events.jsonl*
*.bak*

# S5 thin-slice artifacts (contain task text / derived weights)
evals/datasets/distilled/
evals/adapters/

# Harvested operator prompts — real session text. Scrubbed, but this repo is
# public and the rows still reveal work patterns. Tooling is committed; the
# harvested data is not, unless the owner reviews and opts in.
evals/datasets/eval_candidates.jsonl
evals/datasets/eval_provisional.jsonl
evals/datasets/eval_review_queue.jsonl
evals/datasets/eval_rejected_nontasks.jsonl
evals/datasets/eval_*.jsonl
experiments/perception_cascade/harness/logs/
